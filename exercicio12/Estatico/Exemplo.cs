using System;

namespace Estatico
{
    class Exemplo
    {
        // Atributo estático

        public static string nome = "Aparecido";
        // Método estático
        public static void soma(int n1, int n2)
        {
            Console.WriteLine(n1+n2);
        }
    }
}